// reducerと共通の定数のimportなど

const INITIAL_STATE = {
  inputtsingWord: 'baby baby',
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
